# Redis OM Python — Agent Guide

Object mapping library for Redis built on Pydantic v2, using Redis Search and Redis JSON features where available. The published package is `pyredis-om`; consumers import `aredis_om` for async APIs and generate/use `redis_om` for sync APIs.

## External research conventions

- When project documentation lookup is needed, use Context7 if available.
- When web search is needed, use one search call at a time to avoid rate limits. (`brave-web-search`). Fallback to regular fetch if it fails.

## Current repository facts

- **Version source:** `pyproject.toml` is authoritative; do not rely on stale internal notes for the current package version.
- **Python support:** `>=3.10,<4.0`; CI currently tests Python 3.10–3.14.
- **Source of truth:** `aredis_om/` is the async implementation.
- **Generated outputs:** `redis_om/` and `tests_sync/` are generated by `make sync` and are ignored by git. They may not exist in a fresh clone until generated.
- **Dependency/tooling manager:** `uv` is used by the Makefile and GitHub Actions.
- **Test dependencies:** `pytest`, `pytest-asyncio`, `pytest-xdist`, `pytest-cov`, `pytest-codspeed`, `bandit`, `mypy`, `black`, `isort`, and `flake8` are installed via `uv sync --extra dev`.
- **Redis test targets:** local Compose exposes `redis:8-alpine` on port 6380 and OSS `redis:latest` on port 6381; CI uses `redis:8-alpine` on port 6379.
- **RESP3 accommodation:** `aredis_om/model/resp3_shim.py` normalises RESP3 dict responses from `FT.SEARCH`, `FT.AGGREGATE`, and `FT.AGGREGATE WITHCURSOR` into the legacy RESP2 flat-pair shape. Tests in `tests/test_protocol_compat.py`, `tests/test_protocol_negotiation.py`, `tests/test_resp3_shim.py`, and `tests/test_protocol_benchmark.py` cover both protocols.

## Repository structure

```text
aredis_om/                 # Async source of truth
├── model/
│   ├── model.py           # RedisModel, HashModel, JsonModel, queries, persistence
│   ├── migrations/        # Migrator and index create/drop logic
│   ├── query_resolver.py  # Expression tree to RediSearch query rendering
│   ├── render_tree.py     # Expression tree rendering helpers
│   ├── token_escaper.py   # RediSearch token escaping
│   └── encoders.py        # JSON/Pydantic-compatible encoding helpers
├── connections.py         # Redis / Redis Cluster connection factories
├── checks.py              # Redis command capability checks and cache
├── _compat.py             # Pydantic v2 compatibility shims
└── util.py                # Type utility helpers

tests/                     # Async tests and benchmarks
redis_om/                  # Generated sync mirror; do not edit by hand
tests_sync/                # Generated sync tests; do not edit by hand
make_sync.py               # unasync generation rules
Makefile                   # Main install/sync/lint/test/dist workflow
docker-compose.yml         # Single-node Redis services
docker-compose.cluster.yml # Six-node local Redis Cluster
```

## Development workflow

1. Edit only `aredis_om/` and `tests/` for source or test changes.
2. Run `make sync` after source/test changes to regenerate `redis_om/` and `tests_sync/`.
3. Use local Redis Stack-compatible Redis for module-dependent tests:
   ```sh
   docker compose up -d
   export REDIS_OM_URL="redis://localhost:6380?decode_responses=True"
   ```
4. Common commands:
   ```sh
   uv sync --extra dev
   make sync
   make lint
   make test
   make test_oss
   make test_cluster
   make dist
   ```
5. Direct pytest examples:
   ```sh
   uv run pytest tests/test_hash_model.py -vv
   uv run pytest tests/test_hash_model.py::test_basic_crud -vv
   uv run pytest -k "json and not oss" -vv
   ```

## Validation notes

- `make lint` currently runs `make dist`, then isort, black, flake8, mypy, and bandit.
- `make test` runs `make sync`, starts Docker Compose, runs async + generated sync tests with coverage, then shuts Compose down.
- `make test_cluster` starts both single-node and cluster Compose files, then runs async + sync cluster operation tests.
- `tox.ini` is currently stale relative to the uv workflow: it uses Poetry commands even though the project no longer has Poetry metadata in the main workflow.
- `uv.lock` is ignored and not present in a fresh clone, while CI cache keys reference it. Treat dependency resolution as unconstrained by a committed lockfile.

## Key architecture

### Connection management (`aredis_om/connections.py`)

- `get_redis_connection(**kwargs)` returns either `redis.Redis` or `redis.RedisCluster`.
- `decode_responses=True` is the default unless explicitly overridden.
- `url=` or `REDIS_OM_URL` drives URL-based connection creation.
- Cluster mode is enabled with `cluster=True` or `cluster=true` in the URL; `_strip_cluster_param()` removes that flag before passing the URL to redis-py.

### Schema and field types (`aredis_om/model/model.py`)

- `RediSearchFieldTypes`: `TEXT`, `TAG`, `NUMERIC`, `GEO`.
- Important mappings:
  - `bool` → TAG
  - `int`, `float`, `decimal.Decimal` → NUMERIC
  - `datetime.date`, `datetime.datetime` → NUMERIC
  - `str` → TAG, plus TEXT when `full_text_search=True`
  - `Coordinates` → GEO
  - Embedded Redis models → recursively generated fields
  - Other types → TAG fallback
- Hash schema generation starts from `HashModel.redisearch_schema()` and `HashModel.schema_for_fields()`.
- JSON schema generation starts from `JsonModel.redisearch_schema()` and `JsonModel.schema_for_fields()`.

### Data conversion pipeline

- **HashModel save:** `model_dump()` → datetime/date to timestamp → bytes to base64 → dataclasses to dicts → `jsonable_encoder()` → Redis hash.
- **JsonModel save:** `model_dump()` → datetime/date to timestamp → bytes to base64 → dataclasses to dicts → Redis JSON.
- **HashModel get/search:** Redis data → empty strings to `None` for optional fields → base64 to bytes → Pydantic validation.
- **JsonModel get/search:** Redis JSON → timestamp to datetime/date → base64 to bytes → Pydantic validation.

### Query path

- Query expressions are built with `Expression`, `NegatedExpression`, `ExpressionProxy`, and `FindQuery`.
- `FindQuery.query`, `FindQuery.expression`, and `FindQuery.pagination` cache rendered query fragments on the instance.
- RediSearch values are escaped through `TokenEscaper` before interpolation into query strings.
- KNN query parameters use RediSearch `PARAMS`; non-wildcard filters are wrapped before appending KNN syntax.
- `FindQuery.iter_cursor()` uses `FT.AGGREGATE WITHCURSOR`, loads `__key`, and fetches models by primary key so cursor pages reuse normal model hydration.
- `FindQueryCursor.token(secret=...)` produces URL-safe cursor tokens; signed tokens should be preferred for web handoff and restored with `FindQueryCursor.from_token(...)`.
- `JsonModel.get_value(pk, field_path, *, raw=False)` retrieves a single field or nested sub-value directly via `JSON.GET key <jsonpath>` without loading the whole document. The `field_path` uses `__`-separated model field names (same syntax as `update()`) or a raw `$`-prefixed JSONPath string. Type conversion (datetime, bytes, etc.) is applied automatically unless `raw=True`.

### Bulk fetch and pipeline support

- `HashModel.get_many(pks, pipeline=None)` batches `HGETALL` calls.
- `JsonModel.get_many(pks, pipeline=None)` batches `JSON.GET` calls.
- `RedisModel.add()` supports bulk save via pipeline.
- `delete_many()` chunks deletes with `more_itertools.ichunked(..., 100)`.
- Cluster pipelines are detected by `_is_cluster_pipeline()` because redis-py cluster pipeline commands must be queued without awaiting the individual command return.
- `JsonModel.get_value(pk, field_path)` for efficient JSON sub-value retrieval via `JSON.GET`; supports `__`-separated field paths and raw `$`-prefixed JSONPath strings.

## Recent feature/fix coverage

- Lazy `Meta.database` resolution, callable database providers, and runtime database reassignment.
- `Meta.default_ttl` support for `save()` and `add()`.
- Embedded JSON sorting through `FindQuery.sort_by()` using dotted or double-underscore paths.
- Index health warnings after `FT.INFO` indicates indexing failures.
- Redis Cluster connection support and cluster-safe migration/index creation.
- `get_many()` bulk retrieval and explicit pipeline composition.
- Binary `bytes` round-tripping through base64 conversion.
- Enum numeric query conversion and numeric `IN` / `NOT_IN` query support.
- Optional `HashModel` field retrieval where Redis stores null-like values as empty strings.
- Custom TAG field separators.
- Embedded model query prefix isolation.
- KNN + OR query syntax wrapping.
- RediSearch cursor pagination through `FindQuery.iter_cursor()` and `FindQueryCursor`.
- Pydantic v2 / strawberry GraphQL `ExpressionProxy` primary-key stripping.
- RESP3 accommodation: protocol-aware parsers, `protocol_version()` helper, URL/`protocol` kwarg passthrough, end-to-end RESP2/RESP3 parity tests, and a RESP2-vs-RESP3 benchmark file.
- `JsonModel.get_value(pk, field_path)` for efficient JSON sub-value retrieval via `JSON.GET`; supports `__`-separated field paths and raw `$`-prefixed JSONPath strings.
- **Redis Streams** via `RedisStream` (XADD/XREAD/XREADGROUP with 8.2 `XACKDEL`/`XDELEX`, 8.4 `CLAIM`, 8.6 `IDMP`/`IDMPAUTO`, 8.8 `XNACK`).
- **Hash field TTL** methods on `HashModel` (`set_field_ttl`, `get_field_ttl`, `persist_field`, etc. — Redis 7.4+).
- **Atomic strings** via `AtomicString` (SET IFEQ/IFNE, DELEX IFEQ, DIGEST) and `msetex()` for bulk set-with-expiry (Redis 8.4+).
- **Vector sets** via `VectorSet` (VADD/VSIM/VINFO/VCARD/VDIM/VEMB/VLINKS/VRANDMEMBER/VREM/VSETATTR/VGETATTR — Redis 8.8+).
- **Hot-keys tracker** via `hotkeys_snapshot` (HOTKEYS START/GET/STOP/RESET — Redis 8.6+).
- **Bitmap operators** via `BitmapOps` (BITOP DIFF/DIFF1/ANDOR/ONE — Redis 8.2+).
- **Sorted set aggregations** via `SortedSetOps` (ZUNION/ZINTER with AGGREGATE COUNT — Redis 8.8+).
- **Cluster admin** via `ClusterAdmin` (CLUSTER SLOT-STATS, CLUSTER MIGRATION STATUS/START/STOP/ABORT/LOG — Redis 8.2+ cluster mode).
- **Keyspace notification helpers** via `KeyspaceEvents`, `build_flags`, `enable_keyspace_events` (Redis 2.8+).
- **Explicit logical query operators** — `Or`, `And`, `Not` (in `aredis_om/model/query_resolver.py`) now render real RediSearch query strings (`|`, implicit-AND space, `-` prefix) verified against the official Redis query-syntax docs, and `QueryResolver.resolve()` returns a string. `FindQuery.resolve_redisearch_query` delegates to the operator's `.query` so `Model.find(Or(...))` works end-to-end. Previously these returned placeholder values (a dict / `None`). See [`docs/queries.mdx`](docs/queries.mdx#explicit-logical-operators-or-and-not).
- **Complete TODO cleanup** — all `TODO` comments have been removed from `aredis_om/` and `tests/`. Decisions were made and documented inline:
  - Sets are intentionally unsupported in `is_supported_container_type` (no order preservation in TAG fields).
  - Geo fields are handled via `Coordinates` type separately from `NUMERIC_TYPES`.
  - Bytes values in TAG queries are passed through unchanged (documented rationale).
  - Multi-value vs exact-match TAG ambiguity is documented with a note.
  - Negating `*` (ALL) is explicitly rejected as logically contradictory.
  - Non-IN/NOT_IN sequence right-hand values now raise `QueryNotSupportedError`.
  - `Meta.key_separator` is now configurable (defaults to `":"`) for `index_name`.
  - `DefaultMeta` is confirmed necessary for inheritance and optional-field distinction.
  - `all_pks` decode_responses=False handling is documented.
  - `update()` and `delete()` have documented limitations.
  - `verify_pipeline_response` is documented as intentionally minimal.
  - `FieldInfo` Pydantic hook is documented with upstream-tracking note.
  - `HashModel.schema_for_type` for container types corrected; HashModel rejects list/tuple fields at class-definition time anyway.
  - `schema_for_fields` / `schema_for_type` merge noted as a future refactor candidate.
  - `Note` model comments in tests clarified (TAG indexing, not FTS).

## Embedded model primary-key behavior

Redis OM has two embedded-model patterns with intentionally different `pk` behavior.

| Behavior | `EmbeddedJsonModel` | Embedded `HashModel` with `class Meta: embedded = True` |
| --- | --- | --- |
| Purpose | Dedicated JSON sub-document type | Regular HashModel reused as embedded data |
| `pk` in `model_dump()` | Always excluded | Preserved when explicitly set; excluded when null or query proxy |
| Input stale `pk` handling | Strips embedded `pk` values before validation | Uses normal RedisModel query-proxy stripping |
| Typical use | Structured sub-documents | Embedded objects that need meaningful identifiers |

## Recent bug fixes and deep-review findings (2026-07-01)

A comprehensive line-by-line audit of the codebase was performed against the
Redis documentation. The following issues were found and fixed:

- **`has_redisearch` no longer conflates RedisJSON with RediSearch.**
  The previous implementation returned ``True`` as soon as RedisJSON was
  detected, even if RediSearch was absent. It now checks ``ft.search``
  directly.  (`aredis_om/checks.py`)
- **`__getitem__` / `get_item` off-by-one.**  ``len(cache) >= item``
  allowed accessing index ``item`` when the cache had exactly ``item``
  elements, producing an ``IndexError``.  Changed to ``len(cache) > item``.
  (`aredis_om/model/model.py`)
- **``validate_primary_key`` threshold is intentionally ``> 2``.**
  ``RedisModel`` always declares an inherited ``pk: Optional[str] = Field(primary_key=True)``
  field, so a model with one user-defined ``primary_key=True`` field actually has a
  count of 2.  The ``> 2`` check therefore correctly allows the legitimate
  "override the default pk" pattern (see ``test_primary_pk_exists``) while
  rejecting genuine multi-PK definitions (count >= 3).  An earlier audit
  incorrectly proposed changing this to ``> 1``; that would have broken custom
  primary keys and was reverted.  (`aredis_om/model/model.py`)
- **TEXT field values are now escaped.**  ``EQ`` and ``NE`` on TEXT fields
  previously inserted raw values into double-quoted RediSearch strings.  A
  ``"`` in the value would break the query syntax.  Values are now run
  through ``escaper.escape()``.  (`aredis_om/model/model.py`)
- **Integer primary-key queries use NUMERIC range syntax (intentionally).**
  ``resolve_field_type`` returns TAG for every ``primary_key=True`` field, but
  the schema generator indexes ``int``/``float`` PKs as NUMERIC (see
  ``schema_for_fields``). The query renderer therefore uses NUMERIC range
  syntax ``@id:[5 5]`` for int values, which matches the actual schema.
  An earlier audit incorrectly proposed changing this to TAG syntax
  ``@id:{5}``; that returned zero results against the NUMERIC index and
  was reverted. (`aredis_om/model/model.py`)
- **TAG separator-split queries now join with spaces.**  When a queried
  value contains the TAG separator character, the code splits it and
  concatenated ``@field:{a}@field:{b}`` without separators.  Each segment
  is now joined with a space (implicit AND).  (`aredis_om/model/model.py`)
- **Pagination offset now increments by ``limit``, not ``page_size``.**
  When ``execute(exhaust_results=True)`` paginated through results, it used
  ``query.page_size`` as the offset step, but each page's result count is
  ``limit``.  This caused gaps or overlaps when the two values differed.
  The public ``page()`` and ``all()`` APIs are unaffected (they keep both
  values in sync), but the internal loop is now correct regardless.
  (`aredis_om/model/model.py`)
- **Empty-string ``Meta`` values are intentionally treated as "use default".**
  The metaclass resolves ``model_key_prefix``, ``primary_key_pattern``,
  ``encoding``, and ``key_separator`` with ``if not getattr(...)``, which treats
  the empty string ``""`` as a sentinel meaning "fall back to the inherited
  default." This is required by ``primary_key_pattern = ""`` — an established
  pattern in the codebase (see ``tests/test_oss_redis_features.py`` and the
  ``m`` fixture in ``tests/test_hash_model.py``) that means "use the inherited
  ``{pk}`` pattern." An earlier audit incorrectly proposed changing these to
  ``is None``; that broke key generation (all instances collided on a single
  key) and was reverted. (`aredis_om/model/model.py`)
- **``FindQueryCursor.__anext__`` uses ``collections.deque``.**  The
  previous ``list.pop(0)`` was O(n) per item.  (`aredis_om/model/model.py`)
- **JsonModel schema generation no longer restricts inner-model field
  types to ``str``.**  ``parent_is_model_in_container`` was lumped together
  with ``parent_is_container_type``, causing ``List[Address]`` (where
  ``Address`` has an ``int`` field) to raise a ``RedisModelError``.
  Only bare ``List[str]`` fields are now restricted.
  (`aredis_om/model/model.py`)
- **Comment-code mismatch in ``HashModel.schema_for_type`` removed.**
  The comment claimed ``List[int]`` mapped to ``TAG``, but the code maps it
  to ``NUMERIC``.  HashModel rejects list fields at class-definition time
  anyway; the misleading comment was removed.
  (`aredis_om/model/model.py`)
- **``ExpressionProxy.__eq__`` / ``__ne__`` mypy suppression typo fixed.**
  ``# ty: ignore`` was not a recognized pragma.  (`aredis_om/model/model.py`)

### Remaining known issues

- **``List[datetime]`` silent data corruption.**  ``convert_datetime_to_timestamp``
  converts ``List[datetime]`` items to numeric timestamps on save, but
  ``convert_timestamp_to_datetime`` does not convert the list items back
  to ``datetime`` on load.  Model instances silently return raw timestamps
  for a field typed ``datetime``.  (`aredis_om/model/model.py`)
- **``bytes`` field querying asymmetry.**  ``bytes`` are base64-encoded on
  save but queried as raw ``bytes`` in ``expand_tag_value``.  A ``bytes``
  equality query will never match the stored base64 string.
  (`aredis_om/model/model.py`)
- **``FindQuery.delete()`` swallows all ``ResponseError`` types.**
  Intentionally broad for the documented API contract; callers needing
  stricter error handling should use raw ``DEL`` commands.
  (`aredis_om/model/model.py`)
- **``ReJSON-RL`` type filter in ``JsonModel.all_pks()``** may need updating
  for Redis 8.x which may report the type as ``"JSON"``.
  (`aredis_om/model/model.py`)

## Performance review highlights

See `SECURITY_REVIEW.md` for the full critique. Key performance points for future work:

- Strong baseline: async-first API, generated sync parity, pipeline-backed bulk operations, lazy connection resolution, query-string caching, and dedicated benchmark tests.
- High-impact optimization candidate: `FindQuery.copy()` rebuilds a query from a copied dict; it is used in pagination and should be profiled for large result sets.
- Safety candidate: `FindQuery.execute(exhaust_results=True)` transparently exhausts all pages without a caller-supplied maximum, which can grow memory use unexpectedly.
- Conversion hot path: datetime/date and bytes conversions walk nested structures on save/get/search and may be worth field-aware optimization.
- Benchmark gap: benchmark tests record and print timings but do not enforce regression thresholds in CI.

## Security review highlights

See `SECURITY_REVIEW.md` for the full critique. Key security points for future work:

- RediSearch query values are escaped through `TokenEscaper`, and KNN vectors use RediSearch `PARAMS`.
- Pydantic validation remains the main data-boundary control for model input and Redis-loaded data.
- Bandit and CodeQL are configured, but dependency pinning is intentionally broad and no committed lockfile is present.
- Index migration builds `FT.CREATE` commands from model-derived schema strings; this is low risk for normal model definitions but should remain documented as an internal-only schema path.
- `model_registry` and command capability caches are global mutable state; this is acceptable for import-time registration but not designed for arbitrary concurrent runtime mutation.
- Local cluster Compose disables Redis protected mode and uses host networking for development only; do not treat it as production hardening guidance.
